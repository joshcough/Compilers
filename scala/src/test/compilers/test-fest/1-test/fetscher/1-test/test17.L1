(((eax <- (allocate 9 1))
  (esi <- eax)
  (eax <- (mem esi 0))
  (eax *= 2)
  (eax += 1)
  (eax <- (print eax))
  ((mem esi 4) <- 5)
  ((mem esi 8) <- 7)
  (eax <- (mem esi 4))
  (eax <- (print eax))
  (eax <- (mem esi 8))
  (eax <- (print eax))
  (eax <- (print esi))
  ((mem esi 16) <- 9)
  (eax <- (print esi))))
