(((eax <- (allocate 5 1))
  ((mem eax 4) <- 3)
  (esi <- eax)
  (eax <- (print eax))
  (eax <- (mem esi 0))
  (eax <<= 1)
  (eax += 1)
  ((mem esi 4) <- eax)
  ((mem esi 8) <- eax)
  (eax <- (print esi))))
