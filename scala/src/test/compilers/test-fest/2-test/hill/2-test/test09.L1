;; allocate
(((ecx <- 13)
  (edx <- 19)
  (eax <- 3)
  (edi <- edx)
  (esi <- eax)
  (eax <- (allocate ecx 1))
  (ebx <- eax)
  (eax <- (allocate edi 1))
  ((mem ebx 8) <- 27)
  ((mem eax 8) <- esi)
  (eax <- (print eax))
  (eax <- (print ebx))
  (eax <- 0)))
