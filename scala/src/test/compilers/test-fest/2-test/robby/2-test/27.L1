(((call :L_1))
 (:L_1
  (ebx <- esi)
  (edi <- edi)
  (eax <- (allocate 9 0))
  ((mem eax 4) <- 3)
  ((mem eax 8) <- 5)
  ((mem eax 12) <- 7)
  ((mem eax 16) <- 9)
  (esi <- eax)
  (esi <- (mem esi 0))
  (esi <<= 1)
  (esi += 1)
  (eax <- (print esi))
  (eax <- eax)
  (esi <- ebx)
  (edi <- edi)
  (return)))
