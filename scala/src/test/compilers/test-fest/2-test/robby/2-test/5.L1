(((call :L_1))
 (:L_1
  (ebx <- esi)
  (edi <- edi)
  (eax <- (allocate 5 0))
  ((mem eax 4) <- 21)
  ((mem eax 8) <- 23)
  (esi <- eax)
  (esi <- esi)
  (esi &= 1)
  (esi *= -2)
  (esi += 3)
  (eax <- (print esi))
  (eax <- eax)
  (esi <- ebx)
  (edi <- edi)
  (return)))
