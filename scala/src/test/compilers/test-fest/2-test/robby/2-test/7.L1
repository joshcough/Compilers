(((call :L_1))
 (:L_1
  (ebx <- esi)
  (edi <- edi)
  (esi <- 21)
  (esi <- esi)
  (esi &= 1)
  (esi *= -2)
  (esi += 3)
  (eax <- (print esi))
  (eax <- eax)
  (esi <- ebx)
  (edi <- edi)
  (return)))
