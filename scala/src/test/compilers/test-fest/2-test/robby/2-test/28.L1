(((call :L_1))
 (:L_1
  (esi <- esi)
  (edi <- edi)
  (ecx <- 1)
  (call :f)
  (ebx <- eax)
  (eax <- (print ebx))
  (eax <- eax)
  (esi <- esi)
  (edi <- edi)
  (return))
 (:f
  (esi <- esi)
  (edi <- edi)
  (ebx <- ecx)
  (eax <- ebx)
  (esi <- esi)
  (edi <- edi)
  (return)))
