(((call :L_1))
 (:L_1 (ecx <- 43) (tail-call :f))
 (:f (x <- ecx) (ecx <- x) (edx <- x) (tail-call :g))
 (:g (x <- ecx) (y <- edx) (ecx <- x) (edx <- y) (eax <- y) (tail-call :h))
 (:h
  (x <- ecx)
  (y <- edx)
  (z <- eax)
  (eax <- (print x))
  (d1 <- eax)
  (eax <- (print y))
  (d2 <- eax)
  (eax <- (print z))
  (eax <- eax)
  (return)))
