((let ([x 6])
   (let ([r1 (:g x -1)])
     (let ([r2 (:f r1 999999999 x)])
       (print r2))))
 (:f (x y z)
     (let ([r (+ x y)])
       (- r z)))
 (:g (x y)
     (let ([p (- x y)])
       (< p 8))))
