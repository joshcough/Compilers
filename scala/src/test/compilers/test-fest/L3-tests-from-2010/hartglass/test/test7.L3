((:fun 5)
 (:fun (arg)
       (let ([t (= 0 arg)])
         (if t
             (print 0)
             (let ([a (:h arg)])
               (let ([new (- arg 1)])
                 (let ([b (:fun new)])
                   (let ([c (print b)])
                     (let ([d (print a)])
                       0))))))))
 (:h (arg)
     (let ([x (+ arg 3)])
       (let ([y (+ x 1)])
         (let ([w (print x)])
           (let ([w (print y)]) 1))))))
