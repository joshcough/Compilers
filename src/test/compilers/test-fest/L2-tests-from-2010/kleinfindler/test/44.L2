(((call :L_1))
 (:L_1 (ecx <- 3) (edx <- 5) (eax <- 7) (tail-call :f))
 (:f
  (x <- ecx)
  (y <- edx)
  (z <- eax)
  (eax <- (print x))
  (_ <- eax)
  (eax <- (print y))
  (_ <- eax)
  (eax <- (print z))
  (_ <- eax)
  (ecx <- y)
  (edx <- z)
  (eax <- x)
  (call :g)
  (_ <- eax)
  (eax <- (print x))
  (_ <- eax)
  (eax <- (print y))
  (_ <- eax)
  (eax <- (print z))
  (eax <- eax)
  (return))
 (:g
  (x <- ecx)
  (y <- edx)
  (z <- eax)
  (eax <- (print x))
  (_ <- eax)
  (eax <- (print y))
  (_ <- eax)
  (eax <- (print z))
  (eax <- eax)
  (return)))
