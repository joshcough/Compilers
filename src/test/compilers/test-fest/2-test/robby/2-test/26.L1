(((call :L_1))
 (:L_1
  (ebx <- esi)
  (edi <- edi)
  (eax <- (allocate 21 17))
  (esi <- eax)
  (esi <- (mem esi 0))
  (esi <<= 1)
  (esi += 1)
  (eax <- (print esi))
  (eax <- eax)
  (esi <- ebx)
  (edi <- edi)
  (return)))
