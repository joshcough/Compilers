(((call :L_1))
 (:L_1
  (ebx <- esi)
  (edi <- edi)
  (esi <- 21)
  (esi <- esi)
  (esi &= 1)
  (esi <<= 1)
  (esi += 1)
  (eax <- (print esi))
  (eax <- eax)
  (esi <- ebx)
  (edi <- edi)
  (return)))
