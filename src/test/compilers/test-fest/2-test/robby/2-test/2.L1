(((call :L_1))
 (:L_1
  (ebx <- esi)
  (edi <- edi)
  (esi <- 5)
  (esi >>= 1)
  (eax <- 7)
  (eax >>= 1)
  (eax *= esi)
  (eax += eax)
  (eax += 1)
  (eax <- (print eax))
  (eax <- eax)
  (esi <- ebx)
  (edi <- edi)
  (return)))
