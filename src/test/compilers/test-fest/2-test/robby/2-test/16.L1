(((call :L_1))
 (:L_1
  (ebx <- esi)
  (edi <- edi)
  (eax <- (allocate 9 0))
  ((mem eax 4) <- 3)
  ((mem eax 8) <- 5)
  ((mem eax 12) <- 7)
  ((mem eax 16) <- 9)
  (esi <- eax)
  (eax <- 3)
  (edx <- (mem esi 0))
  (edx <<= 1)
  (edx += 1)
  (cjump eax < edx :oka_5 :die_4)
  :oka_5
  (cjump 0 <= eax :okb_6 :die_4)
  :die_4
  (eax <- (array-error esi 3))
  :okb_6
  (eax >>= 1)
  (eax *= 4)
  (eax += esi)
  (esi <- (mem eax 4))
  (eax <- (print esi))
  (eax <- eax)
  (esi <- ebx)
  (edi <- edi)
  (return)))
