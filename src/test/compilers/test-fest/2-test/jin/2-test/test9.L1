(((esi <- 9)
  (edi <- 11)
  (eax <- (allocate esi edi))
  (eax <- (print eax))
  (esi <- 13)
  (edi <- 15)
  (eax <- (allocate esi edi))
  (eax <- (print eax))
  (eax <- (allocate edi esi))
  (eax <- (print eax))
  (eax <- (allocate esi esi))
  (eax <- (print eax))))