((let ([a 8])
   (let ([b 6])
     (let ([c (+ a 1)])
       (let ([d (* c a)])
         (let ([b (- b d)])
           (let ([e (* a a)])
             (let ([f (+ a a)])
               (let ([f (+ f f)])
                 (let ([g (+ b f)])
                   (let ([g (+ g 19)])
                     (let ([g (+ g a)])
                       (let ([g (* -3 g)])
                         (print g))))))))))))))
