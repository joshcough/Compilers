;; #6 equal to
(((ebx <- 11)
  (esi <- 11)
  (edi <- -17)
  (eax <- ebx = esi)
  (ecx <- ebx = edi)
  (edx <- edi = ebx)
  (ebx <- eax)
  (esi <- ecx)
  (edi <- edx)
  (ebx <<= 1)
  (ebx += 1)
  (eax <- (print ebx))
  (esi <<= 1)
  (esi += 1)
  (eax <- (print esi))
  (edi <<= 1)
  (edi += 1)
  (eax <- (print edi))))


