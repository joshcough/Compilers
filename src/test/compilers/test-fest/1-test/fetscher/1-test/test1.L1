(((ebx <- 5)
  (ecx <- ebx)
  (eax <- (print ecx))
  (ebx <- 5)
  (ebx *= 2)
  (ebx += 1)
  (eax <- (print ebx))
  (ebx <- 5)
  (ebx <<= 1)
  (ebx += 1)
  (eax <- (print ebx))
  (ebx <- -1)
  (ebx *= 2)
  (ebx += 1)
  (eax <- (print ebx))))
