((let ([a (:alloc)])
  (let ([b (alen a)])
    (let ([c (- b 4)])
      (let ([d (print c)])
        (:output a)))))
 (:output (a)
  (let ([v (aref a 1)])
    (print v)))
 (:alloc ()
  (new-array 7 7)))
