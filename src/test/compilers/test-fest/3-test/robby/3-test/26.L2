(((call :L_1))
 (:L_1 (ecx <- 43) (tail-call :f))
 (:f
  (x <- ecx)
  (ecx <- x)
  (edx <- x)
  (call :g)
  (x <- eax)
  (eax <- x)
  (eax += 2)
  (return))
 (:g
  (x <- ecx)
  (y <- edx)
  (ecx <- x)
  (edx <- y)
  (eax <- y)
  (call :h)
  (y <- eax)
  (eax <- y)
  (eax += 2)
  (return))
 (:h
  (x <- ecx)
  (y <- edx)
  (z <- eax)
  (eax <- (print x))
  (d1 <- eax)
  (eax <- (print y))
  (d2 <- eax)
  (eax <- (print z))
  (eax <- eax)
  (return)))
