(((eax <- (allocate 17 1))
  (edx <- 27) ;; store D
  ((mem eax 4) <- edx)
  ((mem eax 16) <- edx)
  (edx <- 29) ;; store E
  ((mem eax 8) <- edx)
  ((mem eax 24) <- edx)
  ((mem eax 28) <- edx)
  ((mem eax 12) <- 21) ;; Store A
  ((mem eax 20) <- 23) ;; Store B
  ((mem eax 32) <- 31) ;; Store F
  (eax <- (print eax))))
