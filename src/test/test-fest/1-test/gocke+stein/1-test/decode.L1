(((eax <- 10)
  (call :decode)
  (eax <- (print eax))
  (eax <- 5)
  (call :decode)
  (eax <- (print eax))
  (eax <- 0)
  (call :decode)
  (eax <- (print eax))
  (eax <- -10)
  (call :decode)
  (eax <- (print eax)))
 (:decode
  (eax *= 2)
  (eax += 1)
  (return)))
