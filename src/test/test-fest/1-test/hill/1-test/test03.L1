;; #3 shift ops
(((esi <- 15)
  (esi >>= 1)
  (eax <- (print esi))
  (esi <- 15)
  (esi <<= 1)
  (esi += 1)
  (eax <- (print esi))
  (esi <- -64)
  (esi >>= 3)
  (esi -= 1)
  (eax <- (print esi))
  (esi <- -64)
  (esi <<= 3)
  (esi += 1)
  (eax <- (print esi))))


