; Test functions
((
  (call :func2)  
  (eax <- (print eax))
  
  (call :func3)
  (eax <- (print eax))
  
  (call :func1)
  (eax <- (print eax))
  )
 (:func1
   (eax <- 3)
   (return))
 (:func2
   (eax <- 5)
   (return))
 (:func3
   (eax <- 7)
   (return))
)
