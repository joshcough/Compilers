(((esi <- 21)
  (eax <- (allocate esi 5))
  (edi <- eax)
  (ebx <- edi)
  (ebx += 20)
  ((mem edi 4) <- 5)
  (eax <- (mem edi 4))
  (eax *= eax)
  (eax -= 5)
  (ecx <- eax)
  ((mem edi 16) <- eax)
  (ecx *= eax)
  (edx <- ecx)
  (ecx -= 1)
  ((mem edi 20) <- ecx)
  (edx -= 1)
  ((mem edi 16) <- edx)
  ((mem edi 8) <- edx)
  (eax <- (print edi))))