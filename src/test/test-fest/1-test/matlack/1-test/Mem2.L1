(((esi <- 21)
  (eax <- (allocate esi 5))
  (edi <- eax)
  (ebx <- edi)
  (ebx += 20)
  ((mem edi 4) <- 9)
  (eax <- 7)
  ((mem edi 8) <- eax)
  (eax <- (mem edi 4))
  ((mem edi 12) <- eax)
  ((mem edi 16) <- 15)
  ((mem edi 20) <- 85)
  (eax <- (print edi))))