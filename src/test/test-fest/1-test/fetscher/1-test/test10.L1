(((eax <- (allocate 41 7))
  (esi <- eax)
  (eax <- (mem eax 0))
  (eax *= 2)
  (eax += 1)
  (eax <- (print eax))
  (eax <- (print esi))
  (eax <- (mem esi 0))
  (eax -= 10)
  ((mem esi 0) <- eax)
  (eax *= 2)
  (eax += 1)
  (eax <- (print eax))
  (eax <- (print esi))))
